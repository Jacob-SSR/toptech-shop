{
  "framework": "nuxtjs",
  "buildCommand": "npm ci && npm run build",
  "installCommand": "npm ci",
  "outputDirectory": ".output",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
