{
  "framework": "nuxtjs",
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": ".output/public",
  "installCommand": "npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}