{
  "framework": "nuxtjs",
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": ".output",
  "installCommand": "npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
